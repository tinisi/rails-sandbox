<%= form_for(@manifesto) do |f| %>
  <% if @manifesto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manifesto.errors.count, "error") %> prohibited this manifesto from being saved:</h2>

      <ul>
      <% @manifesto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :create_date %><br>
    <%= f.datetime_select :create_date %>
  </div>
  <div class="field">
    <%= f.label :human_id %><br>
    <%= f.number_field :human_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
